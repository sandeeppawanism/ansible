- name: sandeepinc
  hosts: local
  connection: local
  vars_files:
  - var.yaml
  tasks:
  - name: print the values
    ansible.builtin.debug:
      msg: we are having {{ COURSE }} course with the {{ DURATION}} Trainer is {{ TRAINER }}
     

